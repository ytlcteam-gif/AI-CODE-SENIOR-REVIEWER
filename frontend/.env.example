# ─── Backend API URL ─────────────────────────────────────────────────────────────
# Local dev: leave UNSET — Vite proxy handles /api/* → localhost:5000
# Production: set to your deployed Railway backend URL (with /api suffix)
VITE_API_URL=https://your-backend.railway.app/api
